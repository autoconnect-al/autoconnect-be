# Deferred Proxy Groups

## Group A: User/Vendor AP Proxies
Status: Migrated locally in `vehicle-api` (no downstream AP proxy calls).

## Group B: Post/Order AP Proxies
Status: Migrated locally in `vehicle-api` (no downstream AP proxy calls).

## Group C: Instagram OAuth Flow
Status: Migrated locally in `vehicle-api` as direct Instagram integration.

## Deferred Endpoint Status
All previously deferred proxy endpoints are now migrated locally.
